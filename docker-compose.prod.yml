version: '3.4'

services:

  masterchef-bot:
    restart: always

    environment:
      API_KEY: "${API_KEY}"
      DEBUG_MODE: "${DEBUG_MODE}"
      DATABASE_NAME: "${DATABASE_NAME}"
      DATABASE_CONNECTION: "${DATABASE_CONNECTION}"

